<h1>Editing deck</h1>
<%= turbo_frame_tag "deck_#{@deck.id}" do %>
  <%= render "form", deck: @deck %>
<% end %>
<br>
<div>
  <%= link_to "Show this deck", @deck %> |
  <%= link_to "Back to decks", decks_path %>
</div>
